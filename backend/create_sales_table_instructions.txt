require('dotenv').config();
const { createClient } = require('@supabase/supabase-js');

const supabaseUrl = process.env.SUPABASE_URL || 'https://jshsryngogxsmdhwzije.supabase.co';
// We need the SERVICE ROLE key to create tables if we were using an API that allowed it,
// but Supabase JS client doesn't support raw DDL (CREATE TABLE) directly without a postgres connection 
// or an RPC function.

// However, I will output the exact SQL query the user needs to run in the SQL editor.
console.log(`
=== COMMENT CRÉER LA TABLE 'sales' DANS SUPABASE ===

1. Sur la page Supabase que vous regardez (https://supabase.com/dashboard/project/jshsryngogxsmdhwzije/editor), 
   regardez le menu noir à gauche.
2. Cliquez sur "SQL Editor" (l'icône ressemble à une page avec une loupe dessus, ou juste au-dessus du "Table Editor").
3. Cliquez sur "New query" (Nouvelle requête).
4. Copiez et collez EXACTEMENT le texte suivant dans la grande zone de texte au milieu :

CREATE TABLE IF NOT EXISTS public.sales (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  date TEXT NOT NULL,
  items JSONB NOT NULL,
  total NUMERIC NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

-- Sécurité basique
ALTER TABLE public.sales ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow anonymous inserts on sales" ON public.sales FOR INSERT TO anon WITH CHECK (true);
CREATE POLICY "Allow authenticated full access on sales" ON public.sales FOR ALL TO authenticated USING (true);


5. Cliquez sur le bouton vert "Run" (Exécuter) en bas à droite de l'écran.
6. C'est fini ! Vous devriez voir "Success" apparaître.

`);
